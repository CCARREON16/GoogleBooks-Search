import React from "react";

function header() {
    return (
        <header>
            <h1>React Book Search!</h1>
            <p>Search Books!</p>
        </header>
    );
}

export default header;